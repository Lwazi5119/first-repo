This is simply a Matrix operations calculator.
It can do operations for ints, doubles or floats.
Operations include: 
Addition(+), 
Subtraction(-), 
Multiplication(*), 
pre increment(++M), 
post increment(M++), 
is Equal?(==), 
and stream operations(<< and >>)

:)
